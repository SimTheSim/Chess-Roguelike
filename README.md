## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`


TODO
Add more artifacts
Add a chess clock
Add public rooms
Add permanence to rejoin a game (account ? cookie ?)